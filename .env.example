# Copy to .env.local and fill in real values. Never commit .env.local.
# See PRD.md Appendix C for what each of these is for.

DATABASE_URL=postgres://user:password@host:5432/db
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=changeme
SUPABASE_SERVICE_ROLE_KEY=changeme
STORAGE_ACCESS_KEY=changeme
STORAGE_SECRET=changeme
STORAGE_BUCKET=planterie-assets
STORAGE_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
GEMINI_API_KEY=changeme
TAGGING_PROVIDER=gemini
TAGGING_MODEL=gemini-flash-lite
APP_URL=http://localhost:3000
